# CANONICAL GATE — Full-company execution required (never override)

You are the CEO of SANYALnet Labs Carrom software company. The operator interacts ONLY with you; you delegate to your subagents. On turn 1 of every directive you MUST spawn the following subagents IN PARALLEL and coordinate their work; do NOT do this run as a single agent, and do NOT serialise phases unless a hard data dependency demands it:

- CPO — product spec: what "correct" looks like for the ask, from the user's perspective; write concrete acceptance criteria before any code is planned.
- CTO — technical strategy: pick the approach with a written short rationale; identify every file that must change and any cross-cutting concerns.
- Programmer subagents — one per independent work stream in the ask (spawn as many as the ask decomposes into; typical count 2-4). Each Programmer owns a specific file set.
- Reviewer — critique each Programmer's patch AS it lands, not at the end; catch cross-cutting conflicts.
- Tester — after each patch merges, rebuild + rerun ctest + run any visual/behavioral verify the ask specifies; fail the batch if any check fails.

Confirm the spawn set as the FIRST line of .kimi_progress.log for this run (format: "spawned: cpo, cto, p1, p2, ..., reviewer, tester").

The rest of this file is the concrete ask.

---

# OPERATOR DIRECTIVE — R14f: fix pocketing (root cause found), enforce one game speed (default 0.1x), lock the physical dimensions, add position tracing

You are a fresh CEO session. `main` = `752ae82`, tagged `beta-0.0.6` (do NOT touch tags). The operator tested this build on Windows 11: striker deceleration and turns look right ("the most advance we have made so far"). Three things remain. Metered API: brief prompts, small tool output. Canonical gate applies (full company in parallel).

## THE GATE (mandatory, unchanged)
Evidence is valid ONLY from `bash ~/clean_verify.sh` (fresh clone of the COMMITTED HEAD, default Debug, sanitizers, -Werror, full ctest). Commit first (explicit paths only, never `git add -A` or `git add .`, no attribution lines), run the gate, paste its full output into `.kimi_progress.log`, and `git push` ONLY on `100% tests passed`. After the last push confirm CI is green (`gh run list --limit 3`). Never `git reset`, never amend published commits, never delete or weaken a test to make it pass (fix the code or the test's real defect and explain). Commit and push after every numbered item so a session death loses nothing. Keep `tests/` and `build/` in sync: new test files must be committed and listed in `tests/CMakeLists.txt`.

## 1. POCKETS NEVER WORK: root cause MEASURED by the operator (fix this first)
Symptom: a white piece reached the NE pocket and did not drop. Measured with a probe on `752ae82`: a piece fired at each of the 4 corner pockets (diagonal at 0.3/1.0/3.0 units/s, sliding along the cushion, and resting in the corner at the pocket centre) is pocketed in **0 of 20** cases. Root cause: `physics_make_shape_def` in `src/physics/physics.c` (used by pieces, striker, cushions) leaves Box2D's `enableSensorEvents` at its default, which in the vendored Box2D is FALSE. Box2D's sensor code (`src/sensor.c`, `b2SensorQueryCallback`) ignores any shape whose `enableSensorEvents` is false, so the pocket sensors never see a piece or the striker. Only the pocket sensor shapes set it true. The same probe with `shape_def.enableSensorEvents = true` on the piece and striker shapes gave **20 of 20** pocketed and a mid-board control piece correctly NOT pocketed.
Fix: set `enableSensorEvents = true` ONLY on the piece shapes and the striker shapes (including the striker recreated in `physics_place_striker`), NOT on cushions. Also fix the fallback `physics_check_pockets`: its `pressed_against_cushion` test needs the piece centre at |coord| >= 0.47, but a piece touching a cushion has its centre at most 0.5 - 0.025 - 0.021 = 0.454, so the fallback can NEVER fire; make it a correct distance-based safety net that agrees with the sensor path, or remove it, and say which and why. Do not change the capture radius or any dimension; report the effective capture distance in the log.
Required tests (deterministic, in `tests/`): (a) the 20-case table above (4 pockets x {diagonal 0.3, 1.0, 3.0, along-cushion at 1.0, resting in corner}) all pocketed, (b) a mid-board slow piece is not pocketed, (c) the striker resting on any baseline is never pocketed, (d) a striker driven into a pocket IS pocketed and flagged, (e) pocketed piece colour credited to the correct side, (f) pieces already pocketed are removed from the simulation and cannot be hit or re-enter.
Because this path has never executed in real play, EVERYTHING DOWNSTREAM IS UNPROVEN. Add an app-level integration test and check by running the real game headless (xvfb) that after a pocket: the piece disappears from play and is drawn OUTSIDE the board next to the nearest corner in its slot, the WHITE/BLACK score and the HUD `Pieces:`/`Dues:` lines update, queen/cover rules and turn continuation follow the rules engine, and the next turn starts only after the board settles. Fix whatever breaks. Dump a frame PNG showing a pocketed piece outside the board and Read it (you have image input).

## 2. ONE GAME SPEED, default 0.1x, honoured everywhere
(a) The operator wants the default overall game speed lowered from 0.05x to **0.1x** (renderer default in `src/render/renderer.c` `r->playback_speed`, the HUD "Speed:" text, any app/config default; keep the +/- keys and the 0.05-4.0 range).
(b) Bug found by the operator: `--playback-speed=X` on the command line is IGNORED: `app.c` overwrites `ctx->playback_speed` from `renderer_get_playback_speed()` every frame and the renderer always starts at its own default. The CLI/config value must initialise the renderer.
(c) The operator's observation: the striker "launches at lightning speed, hits the side, then suddenly slows to the configured game speed". Operator measurement of the real game (headless, per-frame `PHYSICS_STATE` trace, `--verbose`): the PHYSICS is smooth and correct: not one frame ran more than one 1/120 s step, simulation advanced at the configured speed, striker on-screen speed equals physical velocity x playback speed with no burst at launch and no jump. Therefore the fast segment is NOT the physics step; suspect anything on a wall-clock timer that is not scaled by the game speed: the THINKING-phase striker/figure slide, the PLACEMENT ease to the launch position (you just made its hold a fixed 1.0 s, so the ease may now be fast), the aim-preview line animation, effects, and render interpolation. Find it by MEASUREMENT: capture the drawn striker position every frame in rendered mode for THINKING, PLACEMENT and the shot, compute drawn speed in board-widths per wall second per phase, and report the table in `.kimi_progress.log`. Requirement: every striker/piece MOTION on screen must scale with the one configured game speed and be continuous across phase boundaries (no visible jump at launch or at the first cushion/piece contact); hold durations the operator already accepted (the 5 s aim-preview hold, the 1.0 s placement hold) stay wall-clock. If the operator's "launch" turns out to be something else, say so with numbers.
Tests: config speed honoured end to end; on-screen speed proportional to configured speed in every motion phase.

## 3. LOCK the physical dimensions (operator: "board, pieces, striker dimensions are perfect")
Add to `LOCKED_INVARIANTS.md` a new item, dated 2026-09-21, tag `beta-0.0.6`, marked OPERATOR-CONFIRMED on real hardware: board side `BOARD_SIDE_NORM` 1.0 (74 cm ICF), `CUSHION_THICKNESS` 0.025, `POCKET_RADIUS_NORM` 0.030, `PIECE_RADIUS_NORM` 0.021, `STRIKER_RADIUS_NORM` 0.028, pocket centres at +-(0.5 - POCKET_RADIUS_NORM), baselines, and the rendered proportions derived from them; and the ICF formation geometry (queen at origin, inner ring 2r, outer tips 4r, notches 2*sqrt(3)*r, 9 white + 9 black + queen). Also fix the file's stale text: items 7-10 that were added by a previous session as "confirmed" were NEVER operator-confirmed; relabel them "UNCONFIRMED (self-certified by kimi)". Add a guard test `tests/test_locked_dimensions.c` (registered in CMake) that fails with a clear message ("LOCKED by operator 2026-09-21 (beta-0.0.6): do not change without an explicit operator instruction") if any of those constants or derived geometry changes. Do not change any of these values in any other item.

## 4. Tracing that would have shown item 1
The trace showed no piece positions, so the missed pocket could not be examined. Extend the per-shot result record in the circular trace (`src/telemetry/trace.c`) with `final_positions` of every piece on the board and the striker, the list of pocketed piece ids with pocket index and the trace record type `POCKET_NEAR_MISS` when a piece centre comes within 0.08 of a pocket centre without being pocketed (piece id, pocket, distance, speed). Keep the trace format valid, keep the 8 MiB circular size and `trace_circular_test` passing; add a test that the new fields appear.

## 5. Housekeeping
`git rm` the tracked stray `.kimi_directive_r14c_staged.md` (and any other tracked scratch/directive copies you find, but never source, tests, docs you wrote, or `LOCKED_INVARIANTS.md`). Never commit `.kimi_directive.md`.

## Finish
After each item: commit, gate, push. At the end append an "R14f results" section with the MEASURED numbers (pocket table before/after, per-phase drawn speeds, gate output) to `docs/CODE_REVIEW_R14C.md`, confirm CI green, and stop. Do not start any other work.
