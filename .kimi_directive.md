# OPERATOR DIRECTIVE — Center the title and footer text

You (CEO) are at commit `72d834f` on `main`. Layout is 95% correct — board fits, HUD in sidebar, human figures render, footer band exists. Two remaining defects visible in the operator's fresh capture:

1. **Title "SANYALnet Labs Carrom Arena"** is still right-aligned at the top of the window. Must be **horizontally centered**.
2. **Footer content** (blog link `https://blog.sanyalnet.com/carrom` and `© Supratim Sanyal`) is right-aligned. Must be **horizontally centered**.

## The fix

In `src/render/renderer.c` (or wherever title/footer draw), the text positioning must use:

```c
int text_w = MeasureText(TITLE_TEXT, title_font_size);
int x = (window_width - text_w) / 2;    // center
DrawText(TITLE_TEXT, x, title_y, title_font_size, title_color);
```

Same for the blog link and copyright — use `MeasureText` and `(1000 - text_w) / 2` for x. Do NOT hard-code the x to `1000 - text_w - padding` (which is right-align) or to `padding` (which is left-align).

## Verify

1. `./scripts/kill-all-runs.sh`
2. `cmake --build build --parallel`
3. `cd build && ctest --output-on-failure` — still all green.
4. Fresh capture:
   ```
   xvfb-run -a -s '-screen 0 1920x1080x24' ./build/carrom_arena --mode=capture --seed=7 --headless --frames=30 --capture-dir=/tmp/vis_center
   identify /tmp/vis_center/frame_000015.png   # 1000x600
   ```
5. Visually confirm from the raw PNG that title and footer are centered before claiming done. Read the PNG yourself (use `identify` for dims; if you have `xxd` or a small python script use it to verify pixel columns of the title text are symmetric around x=500). Do NOT just trust that your code change compiled.

## Commit + push — MANDATORY final step

```
git add -A -- src/
git commit -m "Center title and footer text horizontally"
git push
```

This must be the final action of the `-p` run. Do NOT exit without pushing.

## Constraints

- Do NOT touch `beta-0.0.1` tag.
- All work in project subtree.
- No attribution lines.
- Push only to origin.

## Reporting

One line to `.kimi_progress.log` when done.

Begin now.
