# OPERATOR CONTINUATION DIRECTIVE — Phases R2, R3, R4

You (CEO) are resuming Carrom Arena work at git commit `050d715` on `main` (already pushed to `origin`). Phase R1 is DONE and verified — do NOT re-diagnose or re-visit R1 unless something breaks.

## What R1 accomplished (do NOT redo)

- `renderer_create` no longer asserts under Xvfb — fallback monitor dims (1920×1080) + `>= 200` clamp before `InitWindow`.
- `regression_test` no longer hangs — split fast/slow tests, reduced `MAX_CANDIDATES` and `MAX_SIM_TIME` for the test target, `test_full_match_headless_bounded_time` now runs 10 shots with the baseline controller.
- Both Debug (ASan) and Release ctest are 6/6 pass in ~11–12 s.
- Capture mode writes 600 frames under Xvfb.

## Scope of THIS run

### Phase R2 (Windows 10) — cross-platform port
Target host: `sanyalnet@192.168.4.75`, project root `D:\SW_DEV\CARROM`, temp root (allowed for scratch OUTSIDE project subtree) `D:\TEMP`. Password `harryseldon`.

Passwordless key-auth from this Linux box → both Windows boxes is INSTALLED (`~/.ssh/id_ed25519` → `C:\ProgramData\ssh\administrators_authorized_keys`). Default shell over SSH is `cmd` for password auth, `bash` (MSYS) for key auth on those boxes; test both and pick whichever is more reliable for the tool you're using. Prefer `sshpass -p harryseldon ssh sanyalnet@192.168.4.75` for consistency.

Steps:
1. Ensure remote dir: `sshpass -p harryseldon ssh sanyalnet@192.168.4.75 "if not exist D:\SW_DEV\CARROM mkdir D:\SW_DEV\CARROM"`.
2. Sync the source tree. Two options — try in this order:
   - (a) `git clone https://github.com/tuklusan/carrom-arena D:/SW_DEV/CARROM` executed inside the Windows shell (fastest, cleanest — repo is public).
   - (b) `rsync -e "sshpass -p harryseldon ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" -a --exclude 'build*/' --exclude '.kimi_run/' --exclude 'traces/' --exclude 'captures/' /home/sanyalnet/SOFTWARE-DEVELOPMENT/carrom/ sanyalnet@192.168.4.75:/cygdrive/d/SW_DEV/CARROM/` — if rsync exists on the remote.
3. Probe toolchain (in a Windows shell that has PATH set right — try MSYS bash first; if `cl.exe` not found, try `cmd /c "where cl"` from a VS Dev CMD env):
   ```
   where cmake
   where ninja
   where git
   where cl        # MSVC
   where gcc       # MinGW/MSYS
   ```
   Choose the best available toolchain in this preference order: MSVC (Visual Studio 2022) > MinGW-w64 > MSYS gcc. Do NOT install anything; both boxes already have dev tools per the operator.
4. Configure + build: `cmake -B build -DCMAKE_BUILD_TYPE=Release` then `cmake --build build --parallel`. If a raylib/box2d FetchContent step needs internet, it should succeed — the Windows boxes have internet. If specific compile errors appear (Windows-only), fix them minimally and document each in the eventual QA cert.
5. Run ctest: `cd build && ctest --output-on-failure -C Release`. Capture the entire transcript to `EVIDENCE/windows10_ctest.log` (create `EVIDENCE/` at repo root on Linux and populate it via scp from Windows).
6. If any test that passes on Linux fails on Windows, treat it as a real defect — spawn a Programmer subagent, fix it, rebuild, retest until 6/6 pass.

### Phase R3 (Windows 11)
Target: `vagab@192.168.4.103`, project root `C:\Users\vagab\SOFTWARE-DEVELOPMENT\carrom`. Same steps as R2. Log to `EVIDENCE/windows11_ctest.log`.

### Phase R4 (cross-platform certification)
1. Write `CROSS_PLATFORM_QA_CERTIFICATE.md` at repo root, table:
   | Host | OS / kernel | Compiler + version | CMake + version | ctest result | Wall time | Verdict |
2. Update `README.md`: mark cross-platform verified for Linux + W10 + W11; keep any remaining known limitations honest.
3. `git add -A && git commit -m "R2..R4: Windows 10 + Windows 11 verified; cross-platform QA cert" && git push`.

### Do NOT start Phase R5 (GitHub 18-runner matrix) in this run.

## Hard constraints (unchanged)

- All project files stay within the designated project subtree on every host.
- Do NOT modify anything outside the project subtree on the Windows boxes.
- Do NOT weaken existing tests to make them pass. If Windows uncovers a real bug, fix it.
- No new git remotes; push only to existing `origin` (github.com/tuklusan/carrom-arena).
- Do NOT add attribution lines to any commit message.

## Parallelism (unchanged)

R2 and R3 have NO data dependency on each other — spawn Programmer #1 (W10) and Programmer #2 (W11) IN PARALLEL. Reviewer critiques both as they arrive. Tester consolidates the two EVIDENCE logs.

## Reporting

Append one status line every ~10 min to `.kimi_progress.log` at repo root (format: `<ISO ts> <phase> <one-line summary>`).

Begin R2 and R3 in parallel now.
