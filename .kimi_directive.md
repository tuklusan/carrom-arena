# OPERATOR RESUME DIRECTIVE — Carrom Arena, continue toward goal

You (CEO) are resuming work paused at git commit `f87c043` on `main`. Read `RESUME.md` at the repo root FIRST — it is the authoritative handoff. Do NOT re-do the Phase-1 audit.

## Goal (unchanged)

Fully working, presentable, autonomous Carrom Arena, proven on **Linux** and **Windows**. Cross-platform verified on real hosts (list below). GitHub Actions matrix (18 runners) is a follow-up; do not start it in this run.

## Hosts and credentials (as of resume)

All passwords are `harryseldon`. Use `sshpass` (installed at /usr/bin/sshpass on this Linux box... actually verify: `which sshpass` — if absent, `sudo apt-get install -y sshpass`; if sudo unavailable, fall back to `expect` or ProxyCommand-hosted auth. The Linux public key at `~/.ssh/id_ed25519.pub` is already in `C:\ProgramData\ssh\administrators_authorized_keys` on both Windows boxes — passwordless SSH should work.)

- **Linux (this box)**: `sanyalnet@192.168.4.76`, project root `~/SOFTWARE-DEVELOPMENT/carrom`.
- **Windows 10**: `sanyalnet@192.168.4.75`, project root `D:\SW_DEV\CARROM`, temp root `D:\TEMP`. Default shell over SSH is cmd; MSYS bash is available.
- **Windows 11**: `vagab@192.168.4.103`, project root `C:\Users\vagab\SOFTWARE-DEVELOPMENT\carrom`. Same shell shape.
- macOS (192.168.4.77) is UNREACHABLE with the shared password right now — skip it.

## Scope of THIS resume run — in dependency order

### Phase R1 (Linux): fix the two known defects and finish the build
1. Read `CTO_DIAGNOSIS.md`, `PHYSICS_PLAN.md`, `RENDER_PLAN.md`, `TEST_PLAN.md`, `RESUME.md`. All of the CTO's 7-file plan is already applied (see `git diff 287a227..HEAD --stat`).
2. **Defect A — `renderer_create` height assert under Xvfb**: when `GetMonitorHeight(GetCurrentMonitor())` returns 0 (headless / Xvfb), `req_h = 0*4/5 = 0` then `game_surface = req_h - title_h - copyright_h` goes negative and `InitWindow(w, negative_h, …)` triggers `glfwCreateWindow` assert. Floor `max_w`/`max_h` at a sane fallback (e.g. 1280x720) when GLFW reports 0 or negative, and clamp the final `r->height`/`r->width` to `>= 200` before `InitWindow`. Verify: `xvfb-run -a -s '-screen 0 1920x1080x24' ./build/carrom_arena --mode=capture --seed=42 --frames=300 --capture-dir=captures/after` must succeed and write 300 frames.
3. **Defect B — `regression_test` hangs past 180s**: run it directly, `timeout 60 ./build/tests/test_regression`, and diagnose. Likely an unbounded settle loop with the new physics constants. Add progress logging inside the test (elapsed sim time per shot, shots per match), and a hard `TEST_ASSERT` that any single shot settles in ≤ 3s sim time / 5s wall time. If a genuine physics bug is uncovered, fix physics.c — do NOT relax the assertion.
4. Rebuild `./scripts/clean_build.sh`; run `cd build && ctest --output-on-failure` — must be 6/6 pass.
5. Do a rendered capture: `xvfb-run -a -s '-screen 0 1920x1080x24' ./build/carrom_arena --mode=capture --seed=42 --frames=600 --capture-dir=captures/after`. Visually eyeball the last 3 frames — should show sensibly-arranged pieces + a legible HUD.
6. Commit and push: `git add -A && git commit -m "R1: fix Xvfb assert + test_regression hang; ctest 6/6 pass" && git push`.

### Phase R2 (Windows 10): first foreign platform
1. `ssh sanyalnet@192.168.4.75 "if not exist D:\SW_DEV\CARROM mkdir D:\SW_DEV\CARROM"`.
2. Sync via rsync (from Linux): `rsync -e "sshpass -p harryseldon ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" -a --exclude 'build*/' --exclude '.kimi_run/' --exclude 'traces/' --exclude 'captures/' --exclude '.git/' /home/sanyalnet/SOFTWARE-DEVELOPMENT/carrom/ sanyalnet@192.168.4.75:/cygdrive/d/SW_DEV/CARROM/`. If rsync isn't available on Windows, use `scp -r` or `git clone https://github.com/tuklusan/carrom-arena` inside the Windows shell.
3. On W10, probe toolchain: `where cl`, `where cmake`, `where ninja`, `where git`. Prefer MSVC 2022; if absent, MinGW/MSYS.
4. `cmake -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build --parallel && cd build && ctest --output-on-failure`. All artifacts must live under the project subtree, use `D:\TEMP` for any scratch outside it.
5. Capture the ctest transcript to `EVIDENCE/windows10_ctest.log` (create `EVIDENCE/` if needed).

### Phase R3 (Windows 11): second foreign platform
Same as R2 with root `C:\Users\vagab\SOFTWARE-DEVELOPMENT\carrom`. Capture to `EVIDENCE/windows11_ctest.log`.

### Phase R4 (cross-platform certification)
Write `CROSS_PLATFORM_QA_CERTIFICATE.md` at repo root listing per-host: hostname, OS, compiler+version, cmake+version, configure+build+ctest transcripts (or their paths under `EVIDENCE/`), and PASS/FAIL verdict. Sign with today's date. Update `README.md` "Known Limitations" to mark cross-platform verified (leave the three original limitations if they're still real). Commit and push.

### Phase R5 (do NOT start in this run)
GitHub Actions matrix across 18 runners. Note in the commit message that this is the next work item.

## Hard constraints (unchanged)

- **All project files stay within the designated project directory subtree on every host.**
- Do NOT weaken existing tests to make them pass.
- Do NOT re-declare "delivery"/"QA certificate" for Linux until Phase R1 verification passes cleanly and a rendered-mode capture looks like a real game to a human.
- Do NOT modify anything outside the project subtree on Windows.
- No new git remotes; push only to the existing `origin` (github.com/tuklusan/carrom-arena).
- Do NOT add attribution lines to any commit message.

## Parallelism (unchanged)

Fan out subagents in parallel wherever there is no data dependency. Concretely for R1: Programmer #1 fixes Defect A (renderer.c), Programmer #2 fixes Defect B (test_regression.c + physics.c if needed), Reviewer critiques both, Tester re-runs ctest and capture. Do not serialise these.

## Reporting

Append one status line every ~10 min to `.kimi_progress.log` at repo root (format: `<ISO ts> <phase> <one-line summary>`). The operator tails this file.

Begin now.
