# CANONICAL GATE — Full-company execution required (never override)

You are the CEO of SANYALnet Labs Carrom software company. The operator interacts ONLY with you; you delegate to your subagents. On turn 1 of every directive you MUST spawn the following subagents IN PARALLEL and coordinate their work; do NOT do this run as a single agent, and do NOT serialise phases unless a hard data dependency demands it:

- CPO — product spec: what "correct" looks like for the ask, from the user's perspective; write concrete acceptance criteria before any code is planned.
- CTO — technical strategy: pick the approach with a written short rationale; identify every file that must change and any cross-cutting concerns.
- Programmer subagents — one per independent work stream in the ask (spawn as many as the ask decomposes into; typical count 2-4). Each Programmer owns a specific file set.
- Reviewer — critique each Programmer's patch AS it lands, not at the end; catch cross-cutting conflicts.
- Tester — after each patch merges, rebuild + rerun ctest + run any visual/behavioral verify the ask specifies; fail the batch if any check fails.

Confirm the spawn set as the FIRST line of .kimi_progress.log for this run (format: "spawned: cpo, cto, p1, p2, ..., reviewer, tester").

The rest of this file is the concrete ask.

---

# OPERATOR DIRECTIVE — R14b: address the sensible review comments from `docs/CODE_REVIEW_R14.md` (HEAD 7d3237c)

Work autonomously and quietly, step by step. Metered API: be terse, small tool outputs, no long reasoning dumps. Canonical gate applies (full company in parallel, short self-contained briefs).

The operator's own spot-check of the report found it mixed. Decide per finding; the guidance below is binding.

## REJECT (do NOT apply)
- GL-001 (radii). WRONG: it used ICF diameters as radii. Current normalized radii already match the ICF spec on a 74 cm board (piece 1.6 cm/74 = 0.0216, striker 2.07/74 = 0.028, pocket 2.23/74 = 0.030). Leave `POCKET_RADIUS_NORM`, `PIECE_RADIUS_NORM`, `STRIKER_RADIUS_NORM` unchanged. Record in `docs/CODE_REVIEW_R14.md` an "operator disposition" section marking GL-001 rejected with this arithmetic.

## FIX, in this order (build + ctest after every edit; tree must always compile)
1. GL-002 (highest priority; it is the operator's "initial piece placement is wrong" symptom). `src/game/board.c` lays out pieces from hand-written tables with INITIAL_RADIUS 0.08, a white piece sitting on the queen's spot, and a hack that shoves piece 0 to (0.5R, 0.5R). Replace ALL of it with a generated standard ICF formation: queen (red) exactly at the center (0,0); inner ring of 6 pieces whose centers are at distance 2*PIECE_RADIUS_NORM from the center (touching the queen and each other), alternating white/black; outer ring of 12 pieces touching the inner ring and each other, alternating white/black, per the ICF diagram (three pieces of each colour around the inner ring, six of each in the outer ring; use the standard orientation where the two colours alternate around each ring). Total exactly 9 white + 9 black + queen; ids 0-8 white, 9-17 black, queen id unchanged. Add a unit test asserting: queen at origin, counts/colours, ring distances, adjacent-piece gaps equal to 2*radius within 1e-4, zero overlaps, colour alternation, 4-fold symmetry expectations you can justify, and that piece ids are unique. Remove the hack and the old tables.
2. PH-01: snapshot restore must use the full angle (cos/sin of angle, not half). Add a test that snapshots and restores a rotated piece and compares angle.
3. TEL-04: use-after-free of `controller` in `src/telemetry/replay.c` fallback path. Fix ordering.
4. TEL-01/TEL-02: circular trace wraparound (split record at buffer end, reader ignoring write_offset). Verify by reading the code and by extending `test_trace_circular.c` with a wrap case; fix only if the defect is real, otherwise document why not.
5. TEL-03, AI-02, AI-04: bounds hardening (`snprintf`/length checks, size arrays with MAX_PIECES, clamp max_variants).
6. MA-04: `board_sync_from_physics` stub: implement it correctly if anything calls it, or delete it and its declaration if dead (grep callers first).
7. DEF-005 (phase transition hole after `TURN_BOARD_OVER`): fix only if it can produce a wrong frame; keep behaviour otherwise.
8. ID-01, ID-02, ID-03: replace `TEST_ASSERT_TRUE(true)` stub tests with real assertions (ID-02: a piece placed inside a pocket capture radius with the striker away is pocketed and removed from the sim; a piece outside is not; the striker resting on its baseline is NOT pocketed).
9. AI-01 (RNG snapshot/restore around imperfection noise): analyse. Fix only if it demonstrably breaks trace determinism; otherwise document as intentional with evidence.

## DEFER to the next pass (do not touch now)
PH-02 (pocket detection unification), PH-03, DEF-001/DEF-002/DEF-003/DEF-004 (timing, startup countdown), AI-03, all RND-*, ID-04. Also do not change physics parameters, layout math in `src/render`, or anything in LOCKED_INVARIANTS.md items 1-6.

## Finish
All ctest pass (existing 7 plus new tests). Commit in small logical commits (no attribution lines), then `git push`. Append a short "operator disposition" table to `docs/CODE_REVIEW_R14.md` (finding -> fixed / rejected / deferred / not-a-defect, with one-line evidence) and push it. Log one line per step to `.kimi_progress.log`. If you cannot finish, leave the tree compiling, push what is green, and summarise state in `.kimi_progress.log`. Then stop.
