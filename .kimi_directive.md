# CANONICAL GATE — Full-company execution required (never override)

You are the CEO of SANYALnet Labs Carrom software company. The operator interacts ONLY with you; you delegate to your subagents. On turn 1 of every directive you MUST spawn the following subagents IN PARALLEL and coordinate their work; do NOT do this run as a single agent, and do NOT serialise phases unless a hard data dependency demands it:

- CPO — product spec: what "correct" looks like for the ask, from the user's perspective; write concrete acceptance criteria before any code is planned.
- CTO — technical strategy: pick the approach with a written short rationale; identify every file that must change and any cross-cutting concerns.
- Programmer subagents — one per independent work stream in the ask (spawn as many as the ask decomposes into; typical count 2-4). Each Programmer owns a specific file set.
- Reviewer — critique each Programmer's patch AS it lands, not at the end; catch cross-cutting conflicts.
- Tester — after each patch merges, rebuild + rerun ctest + run any visual/behavioral verify the ask specifies; fail the batch if any check fails.

Confirm the spawn set as the FIRST line of .kimi_progress.log for this run (format: "spawned: cpo, cto, p1, p2, ..., reviewer, tester").

The rest of this file is the concrete ask.

---

# OPERATOR DIRECTIVE — R14d: fix the verified root causes of the broken physics (supersedes R14c Part 2)

You are a fresh CEO session. `main` = `92e8bb1`, CI green, 9/9 tests pass in `build/`. Metered API and sessions have been dying from oversized context: keep tool output small, brief sub-agent prompts, and COMMIT AND PUSH AFTER EVERY LOGICAL STEP (explicit paths only; never `git add -A` or `git add .`; no attribution lines). Always build and run `ctest` in `build/` (Debug, ASan/UBSan, -Werror) and paste the real "tests passed" line in `.kimi_progress.log`. Canonical gate applies (full company in parallel). Do not touch tags, do not rewrite history, LOCKED_INVARIANTS.md items 1-6 stay off limits except where stated below.

The operator has measured the root cause. Your own second review (`docs/CODE_REVIEW_R14C.md`) missed it; do not repeat its speculation.

## Root cause 1 (MEASURED): the striker launches 40-80 times too fast
`physics_apply_shot` (`src/physics/physics.c`, about line 558) computes `speed = power * MAX_SPEED (5.0)` and passes it to `b2Body_ApplyLinearImpulseToCenter` as an IMPULSE. Impulse = mass * delta-v. The striker's mass is about 0.00246 (density 1.0 * pi * 0.028^2), so the resulting velocity is speed / 0.00246 = about 2000 units/s, clamped by Box2D to its 400 units/s maximum. The board is 1.0 unit wide. Measured with a probe on the current code:
```
power 0.1 -> speed after 1 step = 199.62 units/s
power 0.4 -> speed after 1 step = 393.33 units/s
power 0.7 -> speed after 1 step = 389.44 units/s
power 1.0 -> speed after 1 step = 393.33 units/s
```
Intended maximum is 5 units/s (about 4 m/s on a 74 cm board). At 400 units/s the striker crosses the board about 400 times a second, tunnels through pieces, bounces off cushions dozens of times (your own trace showed 29 cushion hits in ONE shot), and looks like it changes direction for no reason. This explains the operator's symptoms: broken physics, weak or absent reactions, "infinite" launches, impossible direction changes.
Fix: make the launch speed exactly `power * MAX_SPEED` in units/s (set the linear velocity directly with `b2Body_SetLinearVelocity`, or use impulse = mass * speed via `b2Body_GetMass`). Audit every other place that applies an impulse or velocity for the same unit error (AI shot planning `src/ai/shot_candidates.c` and `controller.c`, any replay/snapshot path).
Probe to seed the test (public API only; `physics_get_striker_velocity` exists): place the striker on the south baseline, `physics_apply_shot(pw, 1.5707963f, power)`, `physics_step(pw, PHYSICS_DT)`, read velocity.

## Root cause 2 (verify by measurement, then fix): Box2D is running with meter-scale defaults on centimetre-scale objects
`physics_create` uses `b2DefaultWorldDef()` unchanged and never sets length units. With pieces about 0.02 units in size the vendored defaults are far too coarse: `linearSlop` 0.005 (about 25% of a piece radius), speculative distance 0.02 (about one piece radius), `restitutionThreshold` 1.0 units/s (restitution is IGNORED for approach speeds below 1 unit/s, so slow hits are perfectly inelastic: "weak or absent reactions"), `maxContactPushSpeed` 3.0 units/s (overlap resolution can fling pieces), `sleepThreshold` 0.05. Also at 5 units/s the striker moves about 0.042 per 1/120 s step, roughly one piece diameter, so tunnelling is possible without continuous collision.
Do NOT guess. Write measurement tests first and report the numbers in `.kimi_progress.log`, then change only what the measurements prove wrong (candidates: `world_def.restitutionThreshold` to a small value such as 0.05, `maxContactPushSpeed` to about 0.3, `b2SetLengthUnitsPerMeter` scaling, bullet/continuous flag on the striker, `contactHertz`), one change at a time with the tests re-run.

## Root cause 3: the startup countdown (about 15-20 s)
`src/app/app.c` about line 520: `ctx->placement_timer = 1.0 / fmaxf(ctx->playback_speed, 0.05f);` At the default playback speed 0.05x this is 20 SECONDS, shown in the banner "Striker placed at (...) - striking in 20.0s" (`src/render/renderer.c` about line 230). That is the operator's pointless countdown. Fix: the placement hold is a fixed short wall-clock time (1.0 s), independent of playback speed. Keep the banner, halo and text format; only shorten the wait (this touches locked invariant 2 by explicit operator instruction: the long wait is a bug). Also confirm no other timer scales by 1/playback_speed.

## Required deterministic tests (add to `tests/`, all must pass in `build/`)
1. Launch speed: right after `physics_apply_shot` the striker speed equals `power * MAX_SPEED` within 1e-3, for powers 0.1, 0.5, 1.0, and never exceeds MAX_SPEED after one step.
2. Lone striker, full power from a baseline with the board otherwise empty: speed is monotonically non-increasing except at cushion contacts, direction is constant between contacts, it comes to rest within 4 s of simulated time, and it makes at most 5 cushion contacts.
3. Head-on striker to target collision at approach speeds 0.3, 1.0 and 3.0 units/s: momentum transferred (target speed after impact at least 0.6 of the approach speed, striker slows below 0.4 of it); report the measured restitution at each speed.
4. No tunnelling: a full-power striker aimed straight at the rack hits the first piece it crosses (no pass-through).
5. Rack stability: after the initial ICF formation, simulate 3 s with no shot; no piece moves more than 1e-3.
6. Cushion bounce reflects the normal component only (tangential component preserved apart from friction).
7. A piece driven into each of the four corner pockets at realistic speeds (0.5 and 3 units/s) is captured and removed; the striker resting on its baseline is not pocketed.
8. Independence from frame timing: the same shot simulated with frame dt of 1/15 s and 1/60 s (playback speed 1.0) gives the same final positions within tolerance (note `MAX_SUBSTEPS` 4 with 1/120 steps caps catch-up to 4 steps per frame: at 15 FPS and playback speed above 0.25 the simulation silently runs slower than real time and the accumulator grows without bound; fix by looping until the accumulator is drained with a sane cap that DROPS excess time instead of accumulating, and remove the duplicate accumulator layering between `app.c` and `physics_step` if it is safe).

## Finish
All ctest pass in `build/`; CI green after the push (`gh run list --limit 3`); append the root causes and measured numbers to `docs/CODE_REVIEW_R14C.md` and commit it; stop when done. Do not start unrelated work. If you find any of the above claims wrong, say so with numbers rather than forcing it.
