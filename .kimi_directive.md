# CANONICAL GATE — Full-company execution required (never override)

You are the CEO of SANYALnet Labs Carrom software company. The operator interacts ONLY with you; you delegate to your subagents. On turn 1 of every directive you MUST spawn the following subagents IN PARALLEL and coordinate their work; do NOT do this run as a single agent, and do NOT serialise phases unless a hard data dependency demands it:

- CPO — product spec: what "correct" looks like for the ask, from the user's perspective; write concrete acceptance criteria before any code is planned.
- CTO — technical strategy: pick the approach with a written short rationale; identify every file that must change and any cross-cutting concerns.
- Programmer subagents — one per independent work stream in the ask (spawn as many as the ask decomposes into; typical count 2-4). Each Programmer owns a specific file set.
- Reviewer — critique each Programmer's patch AS it lands, not at the end; catch cross-cutting conflicts.
- Tester — after each patch merges, rebuild + rerun ctest + run any visual/behavioral verify the ask specifies; fail the batch if any check fails.

Confirm the spawn set as the FIRST line of .kimi_progress.log for this run (format: "spawned: cpo, cto, p1, p2, ..., reviewer, tester").

The rest of this file is the concrete ask.

---

# OPERATOR DIRECTIVE — Resume R8-2/R8-3 debugging (app.c already restored)

You (CEO) are continuing R8. Status: `9e691c2` (R8-1/R8-4, board centering + footer URL) is safely on main, CI green. Your prior turn broke `src/app/app.c` with a syntax error while adding debug logging and correctly stopped rather than pushing it. The operator has already restored `app.c` to its clean committed state (`git checkout HEAD -- src/app/app.c`) — verified it builds clean again. You are starting this turn from a known-good tree.

Two untracked helper scripts from your own prior work remain on disk: `analyze_behavior.py`, `analyze_board.py`. Reuse them if still valid; a `Testing/` directory (ctest artifacts) is also present and harmless.

## Your own diagnosis from R8_VERIFICATION_REPORT.md (still valid, use it)

Three concrete symptoms, unchanged:
- Figure-striker correlation ~0.0 (mirroring not wired to live striker position)
- Aim line visible in ALL sampled frames (AIM_PREVIEW phase never exits, or the line is drawn unconditionally regardless of phase)
- Figures inside the board bounding box (perpendicular outward offset not held fixed)

## MANDATORY DISCIPLINE — this is why the last attempt corrupted the file

Make ONE small, targeted edit at a time. After EVERY edit to `app.c` (or any file), immediately run:
```
cmake --build build --parallel 2>&1 | tail -20
```
and confirm it compiles cleanly before making the next edit. Do NOT batch multiple changes together and build once at the end — that is what caused the missing-brace corruption last time. If a build fails, fix that specific error before touching anything else; do not layer more changes on top of a broken file.

## STEP 1 — Add phase instrumentation SAFELY

Place debug logging in `renderer_capture_frame` (or an equivalent function already called once per frame in the capture path) rather than deep inside `app.c`'s main loop — this is a safer insertion point per your own note. Gate it behind a new `--debug-phase` CLI flag (default off) to avoid noise in normal runs. Log format, one line per frame to stderr or a file:
```
frame=N phase=<enum name> placement_timer=<value> playback_speed=<value>
```
Build and verify this compiles (per the discipline above) before running anything.

## STEP 2 — Capture and read the phase log

```
./scripts/kill-all-runs.sh
xvfb-run -a -s '-screen 0 1280x720x24' ./build/carrom_arena --mode=capture --seed=7 --headless --frames=180 --capture-dir=/tmp/phase_debug --debug-phase --verbose > /tmp/phase_debug.log 2>&1
```
Read `/tmp/phase_debug.log` and answer definitively:
1. Does the phase ever leave AIM_PREVIEW during 180 frames, or is it stuck there the whole time?
2. At what frame does each transition (THINKING → PLACEMENT → AIM_PREVIEW → SHOT_EXECUTION or equivalent) actually occur?
3. Does `placement_timer` decrement as expected, or is it stuck/reset every frame?

## STEP 3 — Fix based on what the log shows (don't guess)

Likely candidates given your own trace: a phase-transition condition using the wrong enum or comparison operator; the line-drawing code missing a phase check (drawing unconditionally); the figure-mirror code reading a stale/cached striker position instead of the current frame's live value. Fix only what the log evidence points to. One change, one rebuild, confirm compile, then test.

## STEP 4 — Re-verify with your own behavioral check scripts

Re-run `analyze_behavior.py` / `analyze_board.py` (or rewrite if they no longer apply) and confirm:
- `mirror_ok: true` (correlation strongly positive, not ~0.0)
- Figures never inside the board bbox (maintain R8-2's existing pass)
- `aim_line_ok: true`, no velocity_violation (line never coexists with striker motion)
- Line only visible during the CURRENT turn's AIM_PREVIEW window, fully cleared before that turn's SHOT_EXECUTION begins

## STEP 5 — Commit and push

```
cd build && ctest --output-on-failure && cd ..
git add -A -- src/ tests/
git commit -m "R8-2/R8-3: fix phase-gating bug for figure mirror and aim-line correctness"
git push
```

If you cannot fully resolve this within a reasonable number of attempts, STOP cleanly (as you correctly did last time), write a precise summary of what you tried and what remains broken to `.kimi_progress.log`, and exit — do not push anything that doesn't build, and do not run indefinitely.

## Constraints

- Do NOT touch beta-0.0.1, beta-0.0.2, or beta-0.0.3 tags.
- All work in project subtree.
- No attribution lines.
- Push only to origin.
- `./scripts/kill-all-runs.sh` before every local carrom_arena invocation.
- Rebuild after every single edit — no exceptions.

## Reporting

Log every meaningful step, with concrete evidence, to `.kimi_progress.log`.

Begin now.
