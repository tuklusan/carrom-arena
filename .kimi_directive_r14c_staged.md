# OPERATOR DIRECTIVE — R14c: thorough second code review, then fix the defects it finds

Work autonomously and quietly, step by step. Metered API: be terse, small tool outputs (except the required full-file reads), no long reasoning dumps. Canonical gate applies. Base: current `main` (R14b landed). Read `LOCKED_INVARIANTS.md` first (items 1-6 are off limits unless a fix unavoidably requires it).

## Why a second review
The first review (`docs/CODE_REVIEW_R14.md`) missed the causes of the operator's open symptoms and contained an error (GL-001). On real Windows 11 hardware the operator still sees: (a) physics is wrong: strikers/pieces change direction or speed for no physical reason, launches that seem never to settle, weak or absent reactions on collision; (b) pieces that reach a pocket bounce back instead of dropping; (c) a long (about 15 s) countdown at game start. Physics law: a piece moves in a straight line, only decelerating, until it hits a piece, a cushion, or drops into a pocket.

## Part 1: review (no code changes until it is written)
Review the code paths that decide those symptoms, line by line, untruncated (read sequentially in chunks of at most 250 lines, log chunk ranges per file; anything not fully read is reported UNREVIEWED). Do not re-review files unrelated to the symptoms. Mandatory targets:
1. `src/physics/physics.c` and `.h`: world setup, the fixed-step accumulator (PHYSICS_DT 1/120, MAX_SUBSTEPS 4: what happens when the wall-clock dt is large or `playback_speed` is small; is simulated time ever dropped or replayed?), the manual board-resistance loop (`BOARD_COULOMB`, `BOARD_VISCOUS` applied after each substep: units, applied to every body each substep or only moving ones, applied before/after `b2World_Step`, applied twice anywhere, applied to bodies at rest, sign errors so speed can reverse or direction can change), restitution/friction values, sleeping/awake handling, `physics_apply_shot` impulse vs velocity, striker placement and its epsilon, striker body creation/destruction, pocket sensors and `physics_check_pockets`/`physics_check_pocket_events` (both paths: capture radius versus cushion geometry; why an approaching piece could bounce), cushion shapes and thickness versus piece radius.
2. `src/app/app.c` and `app.h`: how simulated time is advanced each frame (`playback_speed`, frame dt clamping, any place where velocity or position is modified outside physics), settle detection and turn advance, every timer (`thinking_min_wall`, `placement_timer`, aim preview, startup sequence): find the REAL source of the roughly 15 s startup countdown (instrument and match the on-screen value), and any code that re-applies a shot or touches body velocity outside `physics_apply_shot`.
3. `src/ai/controller.c` and `shot_candidates.c`: how a chosen plan becomes an impulse; anything that could apply it more than once or with wrong units.
4. `src/render/board_view.c`: where positions used to draw pieces come from (physics body or a copy), interpolation/lag that could make correct physics look wrong.
Write findings to `docs/CODE_REVIEW_R14C.md`: severity, `file:line`, evidence (at most 3 quoted lines), why it violates the physics law or an operator symptom, fix. Include a root-cause map for symptoms (a), (b), (c) with the exact mechanism, and a "could not verify" section. Do not accept a claim without reading the code that proves it; do not write speculative findings. Commit the review before starting Part 2.

## Part 2: fix (only after Part 1 is committed)
Fix the defects found, root causes not symptoms, highest impact first. Rebuild and run ctest after every edit. Add deterministic tests that would have caught each fix: (1) a lone striker decelerates in a straight line, speed monotonically non-increasing, reaches rest in bounded time, independent of `playback_speed` and frame dt; (2) head-on and glancing striker/piece collisions conserve momentum within tolerance; (3) a cushion bounce reflects the normal component only; (4) a piece entering a pocket capture zone from several angles (all four corner pockets) is captured and removed; the striker resting on its baseline is not; (5) no body changes direction without a contact event (drive the world for N shots at fixed seeds and assert). Then remove the startup countdown.
Also confirm the R14b initial layout is intact (ICF formation test still passes).

## Finish
All ctest pass. Commit in logical commits (no attribution lines), push to origin, append a disposition table to `docs/CODE_REVIEW_R14C.md`, log one line per step to `.kimi_progress.log`, then stop. If you cannot finish, leave the tree compiling, push what is green, and summarise the state. Do not touch tags. Run `./scripts/kill-all-runs.sh` before any local carrom_arena run.
